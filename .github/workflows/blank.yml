name: Pull File from Local

on:
  schedule:
    - cron: '*/1 * * * *'  # รันทุก 1 นาที

jobs:
  pull-file:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Copy File
      run: cp D:/Desktop/sale/sale.jpeg $GITHUB_WORKSPACE
